language: r
sudo: false
cache: packages

r_packages:
  - covr
before_script:
  - R CMD INSTALL .

r_github_packages:
  - vimc/orderly@i1793

addons:
  apt:
    packages:
      - libv8-dev
      - libapparmor-dev
